<!-- /.mailbox-controls -->
<input type="hidden" name="assignment_id" value="{{assign._id}}"/>
<input type="hidden" name="is_related_temp" value="{{assign.is_related}}"/>
<div class="mailbox-read-message">
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                <label>TOPIC NUMBER</label>
                <p>{{topic.topic_id}}</p>
            </div>
            <div class="form-group">
                <label>NARRATIVE</label>
                <p>{{topic.narrative}}</p>
            </div>
            <!-- /.form-group -->
        </div>
        <!-- /.col -->
        <div class="col-md-6">
             <!-- /.form-group -->
            <div class="form-group">
                <label>TITLE</label>
                <p>{{topic.title}}</p>
            </div>
            <!-- /.form-group -->
            <div class="form-group">
                <label>DESCRIPTION</label>
                <p>{{topic.description}}</p>
            </div>
            <!-- /.form-group -->
        </div>
    </div>
    <div class="row">
            <div class="col-md-6">
                <!-- /.form-group -->
                <div class="form-group">
                    <label>DOC NO</label>
                    <p>{{document.DOCNO}}</p>
                </div>
            </div>
            <!-- /.col -->
            {{!-- <div class="col-md-6">
                <div class="form-group">
                    <label>SUBJECT</label>
                    <p>{{#each document.SUBJECT as |sbj|}}{{sbj}},{{/each}}</p>
                </div>
                <!-- /.form-group -->
                <div class="form-group">
                    <label>DATE</label>
                    <p>{{#each document.DATE as |dt|}}{{dt}},{{/each}}</p>
                </div>
            </div> --}}
            <!-- /.col -->
            <div class="col-md-12">
                <!-- /.form-group -->
                <div class="form-group">
                    <label>TEXT</label>
                    {{#each document.TEXT as |txt|}}<p>{{txt}}</p>{{/each}}
                </div>
                <!-- /.form-group -->
            </div>
            <!-- /.col -->
    </div>
    <!-- /.row -->
</div>